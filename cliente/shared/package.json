{
  "name": "@reservas-vuelos/shared",
  "version": "1.0.0",
  "description": "DTOs, tipos, eventos y validaciones compartidas entre frontend (React) y backend (Node.js + Express + TypeScript) del Sistema de Reservas de Vuelos en Tiempo Real",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "license": "MIT"
}
